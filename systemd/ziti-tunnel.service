# =============================================================================
# 此檔案已棄用，請使用以下分離的服務：
#
#   gNB 側：ziti-tunnel-gnb.service  (run 模式，攔截+轉發)
#   核網側：ziti-tunnel-core.service (run-host 模式，只提供服務)
#   gNB 側 N2 socat：socat-n2-gnb.service
#   核網側 N2 socat：socat-n2-core.service
#
# 安裝方式：
#   make systemd-install
# =============================================================================
